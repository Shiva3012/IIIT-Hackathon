<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.yogainstructorapp"> <!-- Ensure this matches your applicationId -->

    <application android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight"> <!-- Check this is correct -->

        <activity android:name=".MainActivity" <!-- Make sure this matches your main activity class -->android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>


</application> <!-- Correctly closing the application tag -->
    </manifest> <!-- Correctly closing the manifest tag -->
